{
  "apiVersion": "compliance.example/v1",
  "kind": "Control",
  "metadata": {
    "id": "aws.account.setting-equals",
    "version": 1
  },
  "spec": {
    "title": "AWS account settings match policy",
    "purpose": "Verify that AWS account security settings have the effective values mandated by policy.",
    "entrypoint": "data.compliance.controls.aws.account_setting_equals.evaluate",
    "applies_to": [
      "aws-account"
    ],
    "evidence": [
      {
        "type": "aws.account.configuration/v1",
        "id": "observation"
      }
    ],
    "parameters_schema": "parameters.schema.json",
    "defaults": {
      "severity": "high",
      "remediation": "Apply the approved AWS account security configuration."
    }
  }
}
