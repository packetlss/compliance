{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["section", "setting", "minimum"],
  "additionalProperties": false,
  "properties": {
    "section": {"const": "audit_log"},
    "setting": {"const": "retention_days"},
    "minimum": {"type": "number"}
  }
}
