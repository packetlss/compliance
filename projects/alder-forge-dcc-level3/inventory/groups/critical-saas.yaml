# yaml-language-server: $schema=../../../../tooling/schemas/inventory/resource.schema.json
apiVersion: compliance.example/v1alpha1
kind: InventoryGroup
metadata:
  name: critical-saas-administration
spec:
  selector:
    matchLabels:
      company: alder-forge
      access-scope: critical
